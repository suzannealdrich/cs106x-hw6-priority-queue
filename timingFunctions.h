/* * File: timeTrials.h * ----------------------------------------------------- * This module exports just one function that runs a queueADT through * its paces and reports the results to stdout.    */  #ifndef _trial_h#define _trial_h/* * Function: RunTimeTrial * Usage: RunTimeTrial(100); * ------------------------ * Runs a time trial using queues of specified size.  The time trial reports * the amount of time necessary to insert that many elements to a queue, to * remove those elements, to run Heapsort on that size array. All results are * reported in ticks (which are 1/60 of a second) */void RunTimeTrial(int size);#endif